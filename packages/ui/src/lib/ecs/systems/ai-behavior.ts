import type { EcsWorld } from '../world'

/**
 * High-level behavior trees per Kin type. Stub in Phase 2 — the aiWander
 * system handles the bare-minimum Scribe wander. Phase 3 adds the carry
 * loop (Scribes seek motes); Phase 4 adds Warden aggro; Phase 5 adds
 * Delver expedition start logic.
 */
export function aiBehaviorSystem(_world: EcsWorld): void {
  // no-op stub
}
