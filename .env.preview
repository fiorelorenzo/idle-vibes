# ── Preview / staging ─────────────────────────────────────────
# Used by: `npm run build:preview` (Vite mode: 'preview')
# API: staging Cloudflare Worker

VITE_API_URL=https://idle-vibes-api-preview.your-account.workers.dev

VITE_CLOUD_SYNC=true
VITE_PARSER_THROTTLE=1
